import React from 'react'
import {Container,View} from 'native-base'
import {Text} from 'react-native'
export default Fare = ({fare}) => {
	return (
		<Container>
		<Text>
		{fare}
		</Text>
		</Container>
	
    )
}